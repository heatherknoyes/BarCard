<div class="container barCard-body-box vh-100">
  {{!-- <label for="text" class="text-white">Search:</label>
  <input type="text" id="text" class="text">
  <button class="search">Search Recipe</button> --}}
  <h3 class="text-white m-3">Recipe Library</h3>
  {{#each recipes as |drink|}}
  <div class="row m-3">
    <div class="card">
      <a href="/recipe/{{drink.id}}" class="card-header {{#if drink.is_alcoholic}}bg-success text-white{{else}}text-black{{/if}}">{{drink.drink_name}} {{#if drink.is_alcoholic}}CONTAINS ALCOHOL{{/if}} -  {{lookup ../emoticons @index}}</a>
      <div class="card-body text-black">
        {{drink.instructions}}
      </div>
      <button id="drink_{{drink.id}}" class="drink view-button" data-drink-id="{{drink.id}}">View Recipe</button>
      <div class="card-footer text-muted">
        Posted by {{user.username}} on {{format_date createdAt}}
      </div>
    </div>
  </div>
  {{/each}}

  <script src="/js/recipe.js"></script>
